<!DOCTYPE html>
<html>
  <head>
    <title>Pokedata</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <%= link_to "PokeData", root_path, class: "navbar-brand" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
          <ul class="navbar-nav gap-3">
            <%= link_to "Pokemons", pokemons_path, class: "nav-link" %>
            <%= link_to "Shapes", shapes_path, class: "nav-link" %>
            <%= link_to "Types", types_path, class: "nav-link" %>
            <%= link_to "Abilities", abilities_path, class: "nav-link" %>
            <%= link_to "About Us", about_index_path, class: "nav-link" %>
          </ul>

          <form class="form-inline ml-auto">
            <div class="input-group">
              <input type="text" class="form-control" placeholder="Search for Pokemon" aria-label="Search" aria-describedby="search-addon">

              <div class="input-group-append">
                <select class="form-select rounded-0" id="pokemonTypes" name="pokemonTypes">
                  <option value="" selected>All Types</option>
                  <% Type.all.each do |type| %>
                    <option value="<%= type.name %>"><%= type.name.capitalize %></option>
                  <% end %>
                </select>
              </div>

              <button class="btn btn-success" type="button">Search</button>
            </div>
          </form>
        </div>
      </div>
    </nav>

    <div class="container py-5">
      <%= yield %>
    </div>
  </body>
</html>
